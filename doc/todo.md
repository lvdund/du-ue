
# TODO:

- 14/1/2026:
    - avoid defind const variable type as number which have to print for logging, should use string type instead.
    - do not hard code the behavior of ue -> should defind list of events in order by pass flag in command line or list them in the config file [config file](../config/config.yml)
    - enable multi support cu, du, ue connection. IMPORTANT!!! search all where have tag "FIX"
